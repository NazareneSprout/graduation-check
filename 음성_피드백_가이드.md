# 🎤 음성 피드백 기능 가이드

## 📋 개요

시각장애 학생을 위한 **음성 피드백(TTS: Text-To-Speech)** 기능이 추가되었습니다. 버튼이나 UI 요소를 터치할 때 해당 요소의 텍스트를 음성으로 읽어줍니다.

---

## ✨ 기능 특징

### 1. **자동 음성 안내**
- 모든 클릭 가능한 버튼/UI 요소에 자동 적용
- 터치 시 요소의 텍스트를 한국어 음성으로 출력
- 0.9배속 (명확한 발음)

### 2. **우선순위 시스템**
텍스트 추출 우선순위:
1. `contentDescription` (접근성용 설명)
2. `TextView`의 텍스트
3. `Button`의 텍스트

### 3. **스마트 처리**
- 기존 OnClickListener 유지 (래퍼 패턴)
- 음성 출력 후 원래 동작 실행
- 이전 음성 자동 중단

---

## 🎯 사용자 경험

### 활성화 방법
1. **내 정보** 탭 이동
2. **장애학생 배려 모드** 버튼 클릭
3. **☑ 음성 피드백 (시각장애)** 체크
4. **적용** 버튼 클릭
5. 앱 자동 재시작

### 사용 시나리오

```
시나리오 1: 홈 화면 탐색
┌─────────────────────────┐
│  [졸업분석 시작]          │  ← 터치
└─────────────────────────┘
     ↓
🔊 "졸업분석 시작" (음성 출력)
     ↓
졸업분석 화면으로 이동


시나리오 2: 시간표 관리
┌─────────────────────────┐
│  [시간표 추가]            │  ← 터치
└─────────────────────────┘
     ↓
🔊 "시간표 추가" (음성 출력)
     ↓
시간표 추가 다이얼로그 표시
```

---

## 🏗️ 기술 구현

### 1. BaseActivity.java (핵심 구현)

#### TTS 엔진 초기화

```java
private TextToSpeech tts;
private boolean ttsInitialized = false;

private void initializeTTS() {
    tts = new TextToSpeech(this, status -> {
        if (status == TextToSpeech.SUCCESS) {
            // 한국어 설정
            int result = tts.setLanguage(Locale.KOREAN);

            if (result == TextToSpeech.LANG_MISSING_DATA ||
                result == TextToSpeech.LANG_NOT_SUPPORTED) {
                // 한국어 미지원 시 영어로 대체
                tts.setLanguage(Locale.US);
            }

            // 음성 속도: 0.9배 (명확한 발음)
            tts.setSpeechRate(0.9f);

            // 음높이: 보통
            tts.setPitch(1.0f);

            ttsInitialized = true;
        }
    });
}
```

#### 음성 피드백 활성화 (재귀적)

```java
private void enableVoiceFeedback() {
    getWindow().getDecorView().post(() -> {
        View rootView = getWindow().getDecorView().getRootView();
        addVoiceFeedbackToViews(rootView);
    });
}

private void addVoiceFeedbackToViews(View view) {
    if (view.isClickable() || view instanceof Button) {
        // 기존 리스너 보존
        View.OnClickListener originalListener = getOnClickListener(view);

        // 새 리스너: 음성 출력 + 원래 동작
        view.setOnClickListener(v -> {
            String textToSpeak = getViewText(v);
            if (textToSpeak != null && !textToSpeak.isEmpty()) {
                speak(textToSpeak);
            }

            if (originalListener != null) {
                originalListener.onClick(v);
            }
        });
    }

    // 자식 View도 재귀적으로 처리
    if (view instanceof ViewGroup) {
        ViewGroup viewGroup = (ViewGroup) view;
        for (int i = 0; i < viewGroup.getChildCount(); i++) {
            addVoiceFeedbackToViews(viewGroup.getChildAt(i));
        }
    }
}
```

#### 텍스트 추출 (우선순위)

```java
private String getViewText(View view) {
    // 1순위: contentDescription
    if (view.getContentDescription() != null) {
        return view.getContentDescription().toString();
    }

    // 2순위: TextView
    if (view instanceof TextView) {
        TextView textView = (TextView) view;
        if (textView.getText() != null) {
            return textView.getText().toString();
        }
    }

    // 3순위: Button
    if (view instanceof Button) {
        Button button = (Button) view;
        if (button.getText() != null) {
            return button.getText().toString();
        }
    }

    return null;
}
```

#### 음성 출력

```java
public void speak(String text) {
    if (!ttsInitialized || tts == null) {
        return;
    }

    // 이전 음성 중단
    tts.stop();

    // API 21+ (Android 5.0+)
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) {
        tts.speak(text, TextToSpeech.QUEUE_FLUSH, null, "utteranceId");
    } else {
        tts.speak(text, TextToSpeech.QUEUE_FLUSH, null);
    }
}
```

#### 리소스 정리

```java
@Override
protected void onDestroy() {
    if (tts != null) {
        tts.stop();
        tts.shutdown();
    }
    super.onDestroy();
}
```

---

### 2. 리플렉션을 통한 기존 리스너 보존

**문제**: Android에는 View에서 OnClickListener를 가져오는 공식 API가 없음

**해결**: Java Reflection 사용

```java
private View.OnClickListener getOnClickListener(View view) {
    try {
        // View의 mListenerInfo 필드 접근
        Field listenerInfoField = View.class.getDeclaredField("mListenerInfo");
        listenerInfoField.setAccessible(true);
        Object listenerInfo = listenerInfoField.get(view);

        if (listenerInfo != null) {
            // ListenerInfo의 mOnClickListener 필드 접근
            Field onClickListenerField =
                listenerInfo.getClass().getDeclaredField("mOnClickListener");
            onClickListenerField.setAccessible(true);

            return (View.OnClickListener) onClickListenerField.get(listenerInfo);
        }
    } catch (Exception e) {
        // 실패 시 null 반환 (정상 동작)
    }
    return null;
}
```

**작동 원리**:
1. View의 내부 `mListenerInfo` 객체 접근
2. ListenerInfo의 `mOnClickListener` 필드 가져오기
3. 실패 시 null 반환 (새 리스너 설정)

---

## 📊 데이터 흐름

```
[사용자] "음성 피드백 켜기"
    ↓
[UserProfileFragment] 설정 저장
    ↓ SharedPreferences
    ↓ Firestore
[앱 재시작]
    ↓
[LoginActivity] 설정 로드
    ↓
[BaseActivity.onCreate()] TTS 초기화
    ↓
[BaseActivity.applyAccessibilitySettings()]
    ↓
[enableVoiceFeedback()]
    ↓
[addVoiceFeedbackToViews()] 모든 View 순회
    ↓
각 버튼에 음성 피드백 추가
    ↓
[사용자] 버튼 터치
    ↓
🔊 음성 출력
    ↓
원래 동작 실행
```

---

## 🔧 접근성 최적화 팁

### 1. contentDescription 활용

**권장 사항**: 아이콘 버튼에는 반드시 `contentDescription` 설정

```xml
<!-- ❌ 나쁜 예: 음성으로 "ImageButton"이라고 읽힘 -->
<ImageButton
    android:id="@+id/btnSearch"
    android:src="@drawable/ic_search" />

<!-- ✅ 좋은 예: 음성으로 "검색"이라고 읽힘 -->
<ImageButton
    android:id="@+id/btnSearch"
    android:src="@drawable/ic_search"
    android:contentDescription="검색" />
```

### 2. 의미 있는 버튼 텍스트

```xml
<!-- ❌ 나쁜 예 -->
<Button android:text=">" />  <!-- 음성: "꺾쇠" -->

<!-- ✅ 좋은 예 -->
<Button
    android:text="다음"
    android:contentDescription="다음 단계로 이동" />
```

### 3. 너무 긴 텍스트 피하기

```java
// ❌ 나쁜 예: 음성이 너무 김
String longText = "이 버튼을 클릭하면 졸업 요건 분석 화면으로 이동하여...";

// ✅ 좋은 예: 간결한 설명
String shortText = "졸업 분석 시작";
```

---

## 🧪 테스트 시나리오

### 시나리오 1: 기본 동작

```
1. 음성 피드백 활성화
2. 홈 화면의 "졸업분석 시작" 버튼 터치
3. 확인:
   ✅ "졸업분석 시작" 음성 출력
   ✅ 졸업분석 화면으로 정상 이동
```

### 시나리오 2: 연속 터치

```
1. "시간표" 버튼 터치
   → 🔊 "시간표" 출력
2. 즉시 "공통 캘린더" 버튼 터치
   → 🔊 이전 음성 중단
   → 🔊 "공통 캘린더" 출력
```

### 시나리오 3: 아이콘 버튼

```
1. contentDescription이 있는 검색 아이콘 터치
   → 🔊 "검색" 출력 (✅)

2. contentDescription이 없는 아이콘 터치
   → 🔊 음성 없음 (정상)
```

### 시나리오 4: 한국어 TTS 미지원

```
1. 기기에 한국어 TTS 엔진 미설치
2. 앱 실행
3. 로그 확인:
   "한국어 TTS가 지원되지 않습니다. 영어로 대체합니다."
4. 버튼 터치
   → 🔊 영어 발음으로 출력 (대체 동작)
```

---

## 🐛 문제 해결

### 문제 1: 음성이 출력되지 않음

**증상**: 버튼을 눌러도 소리가 안 남

**해결 방법**:

1. **TTS 엔진 확인**
   ```
   설정 → 접근성 → 텍스트 음성 변환 출력
   - 기본 엔진: Google 텍스트 음성 변환
   - 언어: 한국어
   ```

2. **볼륨 확인**
   - 미디어 볼륨이 0이 아닌지 확인

3. **로그 확인**
   ```
   Logcat 필터: "BaseActivity"
   예상 로그:
   D/BaseActivity: TTS 엔진 초기화 완료
   D/BaseActivity: TTS 한국어 설정 완료
   D/BaseActivity: TTS 음성 출력: 졸업분석 시작
   ```

4. **수동 TTS 테스트**
   ```java
   // Activity에서 직접 호출
   if (this instanceof BaseActivity) {
       ((BaseActivity) this).speak("테스트 음성");
   }
   ```

### 문제 2: 버튼 클릭이 안 됨

**증상**: 음성은 나오는데 버튼 동작이 안 됨

**원인**: 기존 OnClickListener 복원 실패

**해결**:
```java
// Reflection 실패 시 로그 확인
W/BaseActivity: OnClickListener 가져오기 실패 (정상 동작)
```

이 경우 버튼의 리스너가 교체되므로, 음성 피드백을 켠 후 **반드시 앱 재시작** 필요

### 문제 3: 일부 버튼만 음성 출력

**증상**: 어떤 버튼은 음성이 나오고 어떤 버튼은 안 나옴

**원인**:
- 텍스트가 없는 ImageButton
- contentDescription 미설정

**해결**:
```xml
<ImageButton
    ...
    android:contentDescription="버튼 설명" />
```

### 문제 4: 음성 속도가 너무 빠름/느림

**조정 방법**:
```java
// BaseActivity.initializeTTS() 메서드에서
tts.setSpeechRate(0.9f);  // 0.5 ~ 2.0 범위
```

- `0.5f`: 매우 느림
- `0.9f`: 약간 느림 (현재 설정)
- `1.0f`: 보통
- `1.5f`: 빠름

---

## 📱 Android TTS 엔진 정보

### 지원 언어

- **한국어**: ✅ Google TTS (추천)
- **영어**: ✅ 기본 제공
- **기타 언어**: 엔진별 상이

### TTS 엔진 종류

1. **Google 텍스트 음성 변환** (권장)
   - 가장 자연스러운 한국어 발음
   - Play 스토어에서 다운로드 가능

2. **Samsung TTS**
   - 삼성 기기 기본 탑재
   - 한국어 지원

3. **Android TTS**
   - 기본 엔진
   - 한국어 품질 낮음

### TTS 엔진 설치 유도

사용자에게 안내:
```
"더 나은 음성 경험을 위해
Google 텍스트 음성 변환 엔진을 설치해주세요.

설정 → 접근성 → 텍스트 음성 변환 출력"
```

---

## 🔒 개인정보 보호

### TTS 데이터 처리

- **온라인 TTS**: Google TTS (인터넷 필요)
  - 텍스트가 Google 서버로 전송됨
  - 음성 합성 후 결과 반환

- **오프라인 TTS**: 기기 내장 엔진
  - 로컬에서 처리
  - 인터넷 불필요

### 음성 데이터 저장

- ❌ 앱에서 음성 녹음하지 않음
- ❌ TTS 출력 내용 저장하지 않음
- ✅ 설정(on/off)만 Firestore에 저장

---

## 📈 성능 최적화

### 메모리 관리

```java
@Override
protected void onDestroy() {
    if (tts != null) {
        tts.stop();      // 음성 중단
        tts.shutdown();  // 엔진 종료
    }
    super.onDestroy();
}
```

**효과**:
- TTS 엔진 메모리 누수 방지
- 백그라운드 스레드 정리

### 초기화 지연 (Lazy Loading)

```java
// TTS는 음성 피드백 모드가 켜진 경우에만 사용됨
// 하지만 모든 Activity에서 초기화됨 (약간의 오버헤드)

// 최적화 아이디어:
if (voiceFeedbackMode) {
    initializeTTS();  // 필요할 때만 초기화
}
```

---

## 🌐 다국어 지원

### 현재 구현

- 기본: 한국어 (Locale.KOREAN)
- 대체: 영어 (Locale.US)

### 확장 방법

```java
private void initializeTTS() {
    tts = new TextToSpeech(this, status -> {
        if (status == TextToSpeech.SUCCESS) {
            // 시스템 언어에 따라 자동 선택
            Locale systemLocale = getResources().getConfiguration().locale;

            int result = tts.setLanguage(systemLocale);

            if (result == TextToSpeech.LANG_MISSING_DATA) {
                // 언어팩 다운로드 요청
                Intent installIntent = new Intent();
                installIntent.setAction(TextToSpeech.Engine.ACTION_INSTALL_TTS_DATA);
                startActivity(installIntent);
            }
        }
    });
}
```

---

## 🎓 교육적 의의

### 시각장애 학생 지원

- **독립적인 앱 사용**: 스크린 리더 없이도 기본 기능 이용 가능
- **빠른 피드백**: 버튼 터치 즉시 음성 안내
- **직관적인 탐색**: 터치하여 버튼 내용 확인

### 포용적 설계 실현

- 장애인 차별 금지법 준수
- UN 장애인 권리협약 이행
- 디지털 접근성 강화

---

## 🚀 향후 개선 사항

### 1. 음성 속도 조절 UI

```xml
<!-- 설정 다이얼로그에 추가 -->
<SeekBar
    android:id="@+id/seekBarSpeechRate"
    android:max="15"
    android:progress="9" />  <!-- 0.9배속 -->
```

### 2. 음성 타입 선택

- 남성 음성 / 여성 음성
- 음높이 조절

### 3. 자동 화면 설명

```java
// Activity 진입 시 화면 제목 자동 안내
@Override
protected void onResume() {
    super.onResume();
    if (voiceFeedbackMode) {
        speak("졸업 분석 화면입니다");
    }
}
```

### 4. 제스처 지원

- 두 번 탭: 음성 재생
- 세 번 탭: 음성 정지

---

## 📚 참고 자료

- [Android TextToSpeech API](https://developer.android.com/reference/android/speech/tts/TextToSpeech)
- [Accessibility Best Practices](https://developer.android.com/guide/topics/ui/accessibility/principles)
- [Google TTS Engine](https://play.google.com/store/apps/details?id=com.google.android.tts)

---

## 👨‍💻 개발자 노트

### 설계 결정

1. **왜 재귀적으로 구현했는가?**
   - ViewGroup 계층 구조 대응
   - 동적 View 추가 지원
   - Fragment, RecyclerView 등 모든 케이스 커버

2. **왜 Reflection을 사용했는가?**
   - Android에 공식 OnClickListener getter 없음
   - 기존 리스너 보존 필수 (앱 동작 유지)
   - Reflection 실패 시에도 정상 동작 (새 리스너 설정)

3. **왜 음성 출력을 먼저 하는가?**
   ```java
   speak(text);              // 1. 음성 먼저
   originalListener.onClick();  // 2. 동작 나중
   ```
   - 사용자에게 즉각적인 피드백 제공
   - 화면 전환 전에 음성 출력 완료

---

**마지막 업데이트**: 2025-12-04
**작성자**: Claude Code
**버전**: 1.0
